# 🎓 AI Ассистент личного кабинета студента

Демо-продукт AI ассистента для личного кабинета студента, использующий RAG (Retrieval-Augmented Generation) для ответов на вопросы на основе нормативных документов.

## 📋 Описание

Проект представляет собой интеллектуального ассистента, который помогает студентам находить ответы на вопросы на основе нормативных документов (законы, приказы, постановления). Система использует:

- **Гибридный поиск** (FAISS + BM25) для нахождения релевантных фрагментов документов
- **RAG цепочку** для генерации ответов на основе найденного контекста
- **Streamlit** для веб-интерфейса

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка индекса

Перед запуском приложения необходимо создать векторный индекс из документов:

```bash
python faiss_vectorization.py --folder files --embedder sbert
```

Эта команда:
- Обработает все `.md` и `.txt` файлы из папки `files`
- Разобьет документы на чанки
- Создаст векторные представления с помощью SentenceTransformer
- Сохранит индекс в файлы `faiss.index` и `faiss_meta.npy`

**Альтернативные варианты:**

Обработка одного файла:
```bash
python faiss_vectorization.py --file path/to/file.md --embedder sbert
```

Использование OpenAI embeddings (требует API ключ):
```bash
python faiss_vectorization.py --folder files --embedder openai
```
### 4. Запуск приложения

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## 📁 Структура проекта

```
ai-assys/
├── app.py                      # Streamlit приложение
├── rag_chain.py                # RAG цепочка для генерации ответов
├── hybrid_search.py            # Гибридный поиск (FAISS + BM25)
├── faiss_vectorization.py      # Скрипт векторизации документов
├── requirements.txt            # Зависимости проекта
├── files/                      # Папка с документами (.md файлы)
├── faiss.index                 # FAISS индекс (создается после векторизации)
└── faiss_meta.npy              # Метаданные индекса (создается после векторизации)
```

## 🔧 Параметры векторизации

При создании индекса можно настроить параметры:

```bash
python faiss_vectorization.py \
    --folder files \
    --embedder sbert \
    --index faiss.index \
    --meta faiss_meta.npy \
    --max-chars 1200 \
    --overlap 200
```

**Параметры:**
- `--folder` - папка с документами
- `--file` - один файл для обработки
- `--embedder` - метод эмбеддинга: `sbert` или `openai`
- `--index` - путь к файлу индекса (по умолчанию: `faiss.index`)
- `--meta` - путь к файлу метаданных (по умолчанию: `faiss_meta.npy`)
- `--max-chars` - максимальное количество символов в чанке (по умолчанию: 1200)
- `--overlap` - перекрытие между чанками в символах (по умолчанию: 200)

## 💻 Использование

1. Откройте приложение в браузере
2. Введите вопрос в поле ввода
3. Дождитесь ответа от ассистента
4. Просмотрите источники информации в раскрывающемся списке

### Настройки в интерфейсе

В боковой панели можно настроить:
- **Количество релевантных фрагментов (top_k)** - сколько фрагментов документов использовать для генерации ответа (3-10)

## 🛠️ Технологии

- **Python 3.8+**
- **Streamlit** - веб-интерфейс
- **FAISS** - векторный поиск
- **Sentence Transformers** - эмбеддинги
- **BM25** - лексический поиск
- **OpenAI API** - генерация ответов (GPT-4o-mini)
- **NLTK** - обработка текста

## ⚠️ Устранение неполадок

### Ошибка: "faiss.index не найден"

**Решение:** Запустите векторизацию документов:
```bash
python faiss_vectorization.py --folder files --embedder sbert
```

### Ошибка: "streamlit не распознано как команда" (Windows PowerShell)

**Решение:** Используйте альтернативный способ запуска:
```powershell
python -m streamlit run app.py
```

Или убедитесь, что Streamlit установлен:
```powershell
pip install streamlit
```

### Ошибка: "OPENAI_API_KEY не установлен"

**Решение:** 
1. Создайте файл `.env` в корне проекта с содержимым:
   ```
   OPENAI_API_KEY=your_api_key_here
   ```
2. Или установите переменную окружения в PowerShell:
   ```powershell
   $env:OPENAI_API_KEY="your_api_key_here"
   ```

### Ошибка при установке зависимостей

**Решение:** Убедитесь, что используете Python 3.8 или выше:
```bash
python --version
```

### Медленная работа при первом запуске

**Нормально:** При первом запуске модели загружаются из интернета. Последующие запуски будут быстрее благодаря кэшированию.

## 📝 Лицензия

Проект создан в образовательных целях.

## 👤 Автор

AI Assistant для личного кабинета студента

